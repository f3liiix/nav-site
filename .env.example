# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# 数据库配置
# Node.js部署时使用localhost作为主机
# Docker部署时使用db作为主机（Docker服务名）
DATABASE_URL=mysql://nav_user:your_password_here@localhost:3306/nav_site

# MySQL环境变量（用于Docker部署）
MYSQL_DATABASE=nav_site
MYSQL_USER=nav_user
MYSQL_PASSWORD=your_password_here
MYSQL_ROOT_PASSWORD=your_root_password_here

# Redis配置
# Node.js部署时使用localhost作为主机
# Docker部署时使用redis作为主机（Docker服务名）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379

# Next.js配置
NEXT_PUBLIC_APP_VERSION=1.3.1
# 应用运行端口（默认为8080）
PORT=8080